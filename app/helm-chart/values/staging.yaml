# Overrides pour l'environnement staging
app:
  replicaCount: 1
  env:
    APP_ENV: staging
    APP_DEBUG: "true"
    # APP_URL derive de ingress.host (voir helm-chart/templates/deployment.yaml)

autoscaling:
  minReplicas: 1
  maxReplicas: 3

mysql:
  primary:
    persistence:
      size: 2Gi
