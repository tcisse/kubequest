apiVersion: v2
name: kubequest-app
description: Application Laravel — KubeQuest
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: mysql
    version: "9.23.0"
    repository: "https://charts.bitnami.com/bitnami"
    condition: mysql.enabled
