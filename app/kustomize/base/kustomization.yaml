apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# La base definit uniquement les ressources communes.
# Le Helm chart est rendu dans chaque overlay avec le namespace cible.
resources:
  - namespace.yaml
